<nav class="navbar navbar-expand-lg navbar-dark app-navbar sticky-top">
  <div class="container">
    <a class="navbar-brand fw-bold" href="/">
      <i class="bi bi-mortarboard-fill me-2"></i>Student Support Portal
    </a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#mainNav"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="mainNav">
      <ul class="navbar-nav ms-auto align-items-lg-center gap-lg-2">
        <% if (typeof currentUser !== 'undefined' && currentUser) { %>
          <li class="nav-item"><a class="nav-link" href="/dashboard">Dashboard</a></li>
          <li class="nav-item"><a class="nav-link" href="/colleges">Colleges</a></li>
          <li class="nav-item"><a class="nav-link" href="/profile">Profile</a></li>
          <% if (currentUser.role === 'admin') { %>
            <li class="nav-item"><a class="nav-link" href="/admin">Admin</a></li>
          <% } %>
          <li class="nav-item">
            <span class="nav-link text-white-50 d-none d-lg-inline">
              <i class="bi bi-person-circle me-1"></i><%= currentUser.name || currentUser.email %>
            </span>
          </li>
          <li class="nav-item">
            <a class="btn btn-outline-light btn-sm ms-lg-2" href="/auth/logout">
              <i class="bi bi-box-arrow-right me-1"></i>Logout
            </a>
          </li>
        <% } else { %>
          <li class="nav-item"><a class="nav-link" href="/login">Login</a></li>
          <li class="nav-item">
            <a class="btn btn-light btn-sm ms-lg-2" href="/register">Sign Up</a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
